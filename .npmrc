# EdgeLink npm Configuration
# 使用国内镜像加速npm包下载

# 主镜像源（中国区）
registry=https://registry.npmmirror.com

# 备用镜像源配置（如果主镜像失败，手动切换）
# registry=https://registry.npm.taobao.org
# registry=https://registry.npmjs.org

# EdgeLink私有包配置（如果使用私有registry）
# @edgelink:registry=https://npm.edgelink.com/

# 严格的SSL检查
strict-ssl=true

# 包锁定文件
package-lock=true

# 保存精确版本
save-exact=false

# 超时设置（毫秒）
fetch-timeout=60000
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# 日志级别
loglevel=warn

# 进度条
progress=true
