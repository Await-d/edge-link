# EdgeLink Docker Compose Environment Variables
# Copy this file to .env and customize for your environment

# ============================================
# Image Registry and Versioning
# ============================================
# Docker image registry (default: edgelink for local builds)
# For production, use your private registry: registry.example.com/edgelink
REGISTRY=edgelink

# Version tag for images (default: v0.0.0-dev for development)
# For releases, use semantic versioning: v1.2.3
VERSION=v0.0.0-dev

# Git commit SHA (auto-detected by build script)
# COMMIT_SHA=abc1234

# Build timestamp (auto-generated by build script)
# BUILD_DATE=2025-10-21T00:00:00Z

# ============================================
# Development Settings
# ============================================
# Set to 'true' to use pre-built images instead of building locally
# USE_PREBUILT_IMAGES=false

# ============================================
# Database Configuration
# ============================================
POSTGRES_DB=edgelink
POSTGRES_USER=edgelink
POSTGRES_PASSWORD=edgelink_dev_password

# ============================================
# Redis Configuration
# ============================================
# Redis doesn't require authentication in dev mode

# ============================================
# Application Configuration
# ============================================
# Server settings
SERVER_PORT=8080
LOG_LEVEL=debug

# JWT settings (CHANGE IN PRODUCTION!)
JWT_SECRET=dev_jwt_secret_change_in_production

# STUN server
STUN_SERVER_ADDRESS=stun.l.google.com:19302

# ============================================
# Email Provider Configuration
# ============================================
# Email provider: smtp, sendgrid, mailgun, ses
EMAIL_PROVIDER=smtp

# SMTP settings (for Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_USE_TLS=false
SMTP_USE_STARTTLS=true
SMTP_SKIP_VERIFY=false
SMTP_TIMEOUT=10s

# SendGrid settings (alternative)
# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=your-sendgrid-api-key
# SENDGRID_SANDBOX_MODE=false

# Mailgun settings (alternative)
# EMAIL_PROVIDER=mailgun
# MAILGUN_DOMAIN=your-domain.com
# MAILGUN_API_KEY=your-mailgun-api-key
# MAILGUN_BASE_URL=https://api.mailgun.net

# Amazon SES settings (alternative)
# EMAIL_PROVIDER=ses
# AWS_SES_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_SES_CONFIG_SET=optional-config-set

# Email sender configuration
EMAIL_FROM_ADDRESS=noreply@edgelink.com
EMAIL_FROM_NAME=EdgeLink Alert System
EMAIL_REPLY_TO=support@edgelink.com

# Queue and retry configuration
EMAIL_QUEUE_SIZE=1000
EMAIL_MAX_RETRIES=3
EMAIL_RETRY_DELAY=5s

# Rate limiting
EMAIL_RATE_LIMIT=100
EMAIL_RATE_PERIOD=1m

# Template configuration
EMAIL_TEMPLATE_DIR=./templates/email
EMAIL_DEFAULT_LANG=zh-CN

# ============================================
# Port Mappings
# ============================================
# You can customize these to avoid conflicts
POSTGRES_PORT=54320
REDIS_PORT=63790
API_GATEWAY_PORT=18080
FRONTEND_PORT=13000
