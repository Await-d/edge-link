{{- if .Values.topologyService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "edge-link-control-plane.fullname" . }}-topology-service
  labels:
    {{- include "edge-link-control-plane.labels" . | nindent 4 }}
    app.kubernetes.io/component: topology-service
spec:
  replicas: {{ .Values.topologyService.replicaCount }}
  selector:
    matchLabels:
      {{- include "edge-link-control-plane.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: topology-service
  template:
    metadata:
      labels:
        {{- include "edge-link-control-plane.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: topology-service
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: topology-service
          image: "{{ .Values.global.imageRegistry }}{{ .Values.topologyService.image.repository }}:{{ .Values.topologyService.image.tag }}"
          imagePullPolicy: {{ .Values.topologyService.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: {{ .Values.topologyService.service.port }}
              protocol: TCP
          {{- if .Values.topologyService.healthProbes.liveness.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.topologyService.healthProbes.liveness.path }}
              port: grpc
            initialDelaySeconds: {{ .Values.topologyService.healthProbes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.topologyService.healthProbes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.topologyService.healthProbes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.topologyService.healthProbes.liveness.failureThreshold }}
          {{- end }}
          {{- if .Values.topologyService.healthProbes.readiness.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.topologyService.healthProbes.readiness.path }}
              port: grpc
            initialDelaySeconds: {{ .Values.topologyService.healthProbes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.topologyService.healthProbes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.topologyService.healthProbes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.topologyService.healthProbes.readiness.failureThreshold }}
          {{- end }}
          {{- if .Values.topologyService.healthProbes.startup.enabled }}
          startupProbe:
            httpGet:
              path: {{ .Values.topologyService.healthProbes.startup.path }}
              port: grpc
            initialDelaySeconds: {{ .Values.topologyService.healthProbes.startup.initialDelaySeconds }}
            periodSeconds: {{ .Values.topologyService.healthProbes.startup.periodSeconds }}
            timeoutSeconds: {{ .Values.topologyService.healthProbes.startup.timeoutSeconds }}
            failureThreshold: {{ .Values.topologyService.healthProbes.startup.failureThreshold }}
          {{- end }}
          env:
            - name: DATABASE_HOST
              value: {{ include "edge-link-control-plane.fullname" . }}-postgresql
            - name: DATABASE_PORT
              value: "5432"
            - name: DATABASE_NAME
              value: {{ .Values.postgresql.auth.database }}
            - name: DATABASE_USER
              value: {{ .Values.postgresql.auth.username }}
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "edge-link-control-plane.fullname" . }}-postgresql
                  key: password
            {{- range $key, $value := .Values.topologyService.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.topologyService.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
{{- end }}
