{{- if .Values.deviceService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "edge-link-control-plane.fullname" . }}-device-service
  labels:
    {{- include "edge-link-control-plane.labels" . | nindent 4 }}
    app.kubernetes.io/component: device-service
spec:
  replicas: {{ .Values.deviceService.replicaCount }}
  selector:
    matchLabels:
      {{- include "edge-link-control-plane.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: device-service
  template:
    metadata:
      labels:
        {{- include "edge-link-control-plane.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: device-service
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: device-service
          image: "{{ .Values.global.imageRegistry }}{{ .Values.deviceService.image.repository }}:{{ .Values.deviceService.image.tag }}"
          imagePullPolicy: {{ .Values.deviceService.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: {{ .Values.deviceService.service.port }}
              protocol: TCP
          {{- if .Values.deviceService.healthProbes.liveness.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.deviceService.healthProbes.liveness.path }}
              port: grpc
            initialDelaySeconds: {{ .Values.deviceService.healthProbes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.deviceService.healthProbes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.deviceService.healthProbes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.deviceService.healthProbes.liveness.failureThreshold }}
          {{- end }}
          {{- if .Values.deviceService.healthProbes.readiness.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.deviceService.healthProbes.readiness.path }}
              port: grpc
            initialDelaySeconds: {{ .Values.deviceService.healthProbes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.deviceService.healthProbes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.deviceService.healthProbes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.deviceService.healthProbes.readiness.failureThreshold }}
          {{- end }}
          {{- if .Values.deviceService.healthProbes.startup.enabled }}
          startupProbe:
            httpGet:
              path: {{ .Values.deviceService.healthProbes.startup.path }}
              port: grpc
            initialDelaySeconds: {{ .Values.deviceService.healthProbes.startup.initialDelaySeconds }}
            periodSeconds: {{ .Values.deviceService.healthProbes.startup.periodSeconds }}
            timeoutSeconds: {{ .Values.deviceService.healthProbes.startup.timeoutSeconds }}
            failureThreshold: {{ .Values.deviceService.healthProbes.startup.failureThreshold }}
          {{- end }}
          env:
            - name: DATABASE_HOST
              value: {{ include "edge-link-control-plane.fullname" . }}-postgresql
            - name: DATABASE_PORT
              value: "5432"
            - name: DATABASE_NAME
              value: {{ .Values.postgresql.auth.database }}
            - name: DATABASE_USER
              value: {{ .Values.postgresql.auth.username }}
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "edge-link-control-plane.fullname" . }}-postgresql
                  key: password
            {{- range $key, $value := .Values.deviceService.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.deviceService.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
{{- end }}
